import 'package:flutter/material.dart';

final custBoxdesign = BoxDecoration(
    color: Colors.white,
    borderRadius: BorderRadius.circular(30.0),
    boxShadow: [
      BoxShadow(
          color: Color.fromRGBO(0, 0, 0, 0.1),
          blurRadius: 2.0,
          spreadRadius: 2.0),
      BoxShadow(
          color: Color.fromRGBO(0, 0, 0, 0.1),
          blurRadius: 2.0,
          spreadRadius: 2.0),
    ]);
